import {FiX} from "react-icons/fi";function ReelEffects({selected,onSelect,onClose}){const a=["Vintage","Warm","Cool","B&W","Dream","Glow","None"];function pick(x){if(x==="None")onSelect([]);else onSelect(selected.includes(x)?selected.filter(y=>y!==x):[...selected,x])}return <div className="reel-panel"><header><h2>Effects</h2><button onClick={onClose}><FiX/></button></header><div className="effects-grid">{a.map(x=><button key={x} className={selected.includes(x)||x==="None"&&!selected.length?"active":""} onClick={()=>pick(x)}>✦<small>{x}</small></button>)}</div></div>}export default ReelEffects;
